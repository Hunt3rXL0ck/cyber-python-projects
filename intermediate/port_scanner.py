# TODO: Basic port scanner
import socket

def scan_port(host, port):
    # TODO: Implement TCP port check
    pass

if __name__ == "__main__":
    host = input("Enter host: ")
    port = int(input("Enter port to scan: "))
    scan_port(host, port)
