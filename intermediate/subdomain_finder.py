# TODO: Find subdomains using a wordlist
import requests

def check_subdomain(domain):
    # TODO: Loop through wordlist and check subdomains
    pass

if __name__ == "__main__":
    domain = input("Enter domain (e.g., example.com): ")
    check_subdomain(domain)
